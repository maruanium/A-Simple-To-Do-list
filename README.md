
# TODO List (C)

A simple command-line TODO list written in C.

## Features

- Add tasks
- List tasks
- Mark tasks as completed
- Mard tasks as uncompleted (under dev)
- Delete tasks
- Edit tasks
- Save tasks to a file (under dev)
## Build && run

```bash
make && ./todo
```
